# Python
__pycache__/
*.py[cod]
*.so
.ipynb_checkpoints/

# Logs / experiment tracking
*.log
wandb/
.DS_Store

# Editor / local tooling (machine-specific LaTeX paths, etc.)
.vscode/

# LaTeX build artifacts and local-only paper junk
paper/**/*.aux
paper/**/*.bbl
paper/**/*.blg
paper/**/*.fdb_latexmk
paper/**/*.fls
paper/**/*.out
paper/**/*.synctex.gz
paper/**/*.toc
paper/**/*.lof
paper/**/*.lot
paper/**/*.nav
paper/**/*.snm
paper/**/*.vrb
paper/**/*.bak
paper/**/*.pdf
paper/**/*.zip
# Keep source figures (png/svg/pdf under figures/ if needed later via negation)
!paper/figures/**/*.pdf
# IEEE template docs / unused bib dumps from Overleaf export
paper/IEEEtran_HOWTO.pdf
paper/IEEEabrv.bib
paper/IEEEfull.bib
paper/IEEEtranS.bst
paper/README
paper/fig1.png

# Generated by training / feature extraction (regenerable from the licensed
# DAIC-WOZ corpus; never committed — they contain per-session derived data)
src/acoustic-depr-wavlm/cache*/
src/acoustic-depr-wavlm/checkpoints/
src/acoustic-depr-wavlm/manifests/
src/acoustic-depr-wavlm/results_*.json
src/semantic-depr-roberta/checkpoints/
src/semantic-depr-roberta/datasets/processed/
src/semantic-depr-roberta/results/
src/semantic-depr-roberta/figures/
src/semantic-depr-roberta/logs/
src/ctd/outputs/

# Model / tensor files anywhere
*.pt
*.npz

# PDCH per-session derived artifacts (MC-12: licensed corpus, and `origin` is
# public). Regenerable in ~15 s from the corpus via scripts/build_pdch_cache.py.
# Aggregate outputs (output/pdch_ctd_results.*, pdch_feature_degeneracy.csv)
# carry no session identifiers and are committed.
output/pdch/pdch_utterances.csv
output/pdch/pdch_session_report.csv
